import { decimals } from 'ships-client/helpers/decimals';
import { module, test } from 'qunit';

module('Unit | Helper | decimals');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = decimals([42]);
  assert.ok(result);
});
